package demoPackage;

import java.util.Base64;

public class Password {

		String Password = "Sanjana@2009";
		
		
		
		/*
		 * public String getDecodedPassword() { return new
		 * String(Base64.getDecoder().decode(EncodePassword.getBytes())); }
		 */
		
		/* for reference and how it works
		 * String EncodePassword = new
		 * String(Base64.getEncoder().encode(Password.getBytes()));
		 * System.out.println(EncodePassword);
		 * 
		 * String DecodePassword = new
		 * String(Base64.getDecoder().decode(EncodePassword.getBytes()));
		 * System.out.println(DecodePassword);
		 */
		
	

}
